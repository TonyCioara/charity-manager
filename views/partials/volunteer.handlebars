<div class="col-sm-3" style="margin-bottom: 32px;">
    <div class="card">
        <div class="card-body">
            <h4>{{this.name}}</h4>
            <p class="card-text">Volunteering Hours: {{this.volunteerHours}}</p>
            <a href="/charities/{{this.charityId}}/volunteers/{{this._id}}/edit" class="btn btn-light">Edit</a>
        </div>
    </div>  
</div>
